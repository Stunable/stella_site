--- 
## these are nginx inclue task

- name: update apt repo
  action: command /usr/bin/apt-get update

- name: install python-software-properties
  action: apt name='python-software-properties' state=installed

# - name: "add nginx ppa if it ubuntu 10.04 and up"
#   action: command add-apt-repository  -y ppa:nginx/stable
#   # only_if: "$is_ubuntu and $is_10_up"

# - name: update apt repo
#   action: command /usr/bin/apt-get update

- name: install nginx 
  action: apt name=nginx state=installed

# - name: write nginx.conf 
#   action: template src='nginx/templates/nginx.conf.j2' dest='/etc/nginx/nginx.conf'
#   notify:
#     - restart nginx

- name: service nginx 
  action: service name=nginx state=started 



